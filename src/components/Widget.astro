---
---

<div class="widget">
  <slot />
</div>

<style>
    .widget {
        background-color: var(--bg-deep);
        border: 2px solid var(--border);
        border-radius: 12px;
        padding: 1.5rem;
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        /* height: 200px; <-- this was killing you */
    }

    .widget :global(h2) {
        color: var(--text);
        font-size: 1.8rem;
        font-weight: 600;
        border-bottom: 3px solid var(--text-muted);
        padding-bottom: 0.5rem;
        margin: 0;
    }

    .widget :global(p) {
        color: var(--text);
        font-size: 14px;
        margin: 0;
    }
</style>